# Day 1 — Numerical resolution of HPPWM (SciPy-based solvers)
#
# Requires Python 3.10 exactly (see README.md). scipy is pinned tightly
# because Exercise 3/4 patch scipy.optimize with a custom hybrid solver
# (_hybrid_differential_evolution.py) that imports private, unstable
# internal modules (scipy.optimize._optimize, scipy.optimize._constraints,
# scipy._lib._util). Those internals can change between scipy releases,
# so installing a different scipy version may break the patch even though
# nothing in this file changed.
numpy==2.1.3
pandas==2.2.3
scipy==1.13.1
tqdm==4.67.1
